#include "UtilsPagos.h"
/*UtilsPagos::UtilsPagos() {
	
}


UtilsPagos::UtilsPagos() {
	double CalcularMontoPorActividad(const std::string& actividad) {
		if (actividad == "Gimnasio") return 5000.0;
		if (actividad == "Spinning") return 6000.0;
		if (actividad == "CrossFit") return 7000.0;
		if (actividad == "Yoga") return 4500.0;
		return 0.0; // Valor por defecto si no se reconoce la actividad
	}
}

